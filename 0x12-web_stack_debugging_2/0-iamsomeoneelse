#!/usr/bin/env bash
# A Bash script that will run the whoami command on the the first argument passed
# to it. Here am assumming the first command passed to the script is a user
# registerred on the system.
sudo -u "$1" whoami
